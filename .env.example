# Public environment variables (already in .env)
# Get these from your Supabase project settings
VITE_SUPABASE_PROJECT_ID="your-supabase-project-id"
VITE_SUPABASE_PUBLISHABLE_KEY="your-supabase-anon-public-key"
VITE_SUPABASE_URL="https://your-project.supabase.co"

# Private environment variables (required for admin scripts)
# DO NOT commit the actual values - this is just an example!
# Get this from your Supabase project settings > API > service_role key (secret)
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key-here"

# Optional: Set a custom password for the admin user
# If not set, a secure random password will be generated and displayed
ADMIN_PASSWORD="your-secure-password-here"
